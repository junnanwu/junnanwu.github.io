# Mybatis问题

## Mybatis中的转义字符

| 字符 | 转义字符 |
| ---- | -------- |
| `<`  | `&lt;`   |
| `>`  | `&gt;`   |
| `&`  | `&amp;`  |
| `'`  | `&apos;` |
| `"`  | `&quot;` |

也可以使用如下符号将目标代码包裹起来：

```
<![CDATA[ ]]>
```

